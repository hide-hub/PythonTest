

・線形回帰クラスの全組み合わせプロット関数に、表示ではなく保存するというオプションを追加する

・計算した w について、
    − 全変数をどれだけ説明するか
    − トレーニングデータをどれだけ説明するか
    − テストデータをどれだけ説明するか
　を表示するような plot 関数を作る

・L1 Regularization と L2 Regularization を組み込む

・実装している全ての線形回帰を行って、ベストスコアを持つ手法を示す関数を追加

・元データ (X) を訓練データとテストデータに分ける割合を操作可能にする

・元データを渡すのではなく、最初から訓練データをテストデータを別々に渡す操作も可にする

